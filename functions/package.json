{
  "name": "clean-expired-rooms",
  "description": "Firebase function to clean up expired TasteBuddies rooms",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions:cleanExpiredRooms"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.4.1"
  },
  "engines": {
    "node": "18"
  }
}
